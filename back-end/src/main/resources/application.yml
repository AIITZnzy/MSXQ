spring:
  datasource:
    url: jdbc:mysql://localhost:3306/devcsw
    #jdbc连接地址 格式为jdbc:mysql://服务器地址:端口/数据库名
    username: root
    #数据库用户名
    password: Aiit#040629
    #数据库密码
    driver-class-name: com.mysql.cj.jdbc.Driver
    #数据库驱动

  ai:
    openai:
      api-key: sk-uq8kwbzUeyakWlsQxjeMWdVSc3IDQX4obwyxwNMt202XNvnh # 填写实际的API密钥
      base-url: https://xiaoai.plus #路径

  # Redis配置
  redis:
    host: localhost
    port: 6379
    password:  # Redis密码，如果没有密码则留空
    database: 0  # 数据库索引，默认0
    timeout: 3000ms  # 连接超时时间
    lettuce:
      pool:
        max-active: 8  # 连接池最大连接数
        max-idle: 8    # 连接池最大空闲连接数
        min-idle: 0    # 连接池最小空闲连接数
        max-wait: -1ms # 连接池最大阻塞等待时间，-1表示无限等待